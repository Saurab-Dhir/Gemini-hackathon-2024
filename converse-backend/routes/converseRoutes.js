const express = require("express");
const router = express.Router();

// Write your requests here for example:
// router.get('.......', converseController.getAudioFile);
// Here converseController is where you actually declare the function

module.exports = router;
